# 推荐：在 Cloudflare Pages → Settings → Environment variables 配置（构建时注入）
# 可选本地 CLI：复制本文件为 .env.production 后再 npm run deploy

VITE_API_BASE=https://myna-edge-api.<你的子域>.workers.dev
VITE_MYNA_INGEST_TOKEN=
# VITE_MYNA_ADMIN_TOKEN=
